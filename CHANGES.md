# Changes

- Fixed broken link for "Principles & Projects" menu item in `_layouts/default.html`.
  - Removed trailing slash from `/pages/mission/` to match `/pages/terms` pattern (`/pages/mission`).

